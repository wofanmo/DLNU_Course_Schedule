# 教务导入生成独立课表快照

每次教务导入创建一份新的 Schedule（名称可编辑，默认带学期名）并切换为当前课表，不与既有课表合并。

## Considered Options

- **并入当前课表**：去重与冲突语义复杂，且会污染手动添加的课程。
- **覆盖式更新**（同一账号重复导入时更新同一份课表）：需要额外维护「课表 ↔ 教务账号」关联，收益不明确。

## Consequences

- 重复导入会产生多份快照，由用户自行删除旧课表。
- 手动添加课程永远写入当前课表，与导入生成的快照互不干扰。
